import { ComingSoon } from './ComingSoon';
export default function PasswordLab() {
  return (
    <ComingSoon
      eyebrow="Password Lab · in progress"
      title="Analyze your password strength locally — or use the safe pattern builder."
      description="Real-time entropy estimation, brute-force time approximation and pattern detection. Nothing is transmitted, logged or stored."
    />
  );
}
