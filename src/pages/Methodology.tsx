import { ComingSoon } from './ComingSoon';
export default function Methodology() {
  return (
    <ComingSoon
      eyebrow="Methodology · in progress"
      title="See exactly how every score is calculated."
      description="Entropy formulas, weighted scoring, graph traversal, and the finite state machine used for pattern classification — fully documented and reproducible."
    />
  );
}
